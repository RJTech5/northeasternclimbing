class Event {

    constructor(title, postedDate, eventDate, eventTime, url, thumbnail) {
      this.title = title;
      this.postedDate = postedDate;
      this.eventDate = eventTime;
      this.url = url;
      this.thumbnail = thumbnail;

      let HTML = ```
      
      ```
    }
}